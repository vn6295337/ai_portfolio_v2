{
  "table_name": "working_version",
  "description": "Supabase working_version table schema for AI models discovery pipeline (excluding auto-managed fields)",
  "schema_fields": {
    "inference_provider": {
      "type": "text",
      "required": true,
      "description": "Inference provider (e.g., OpenRouter, Groq)"
    },
    "model_provider": {
      "type": "text",
      "required": true,
      "description": "Model provider/company name (e.g., Meta, Google, DeepSeek)"
    },
    "human_readable_name": {
      "type": "text",
      "required": true,
      "description": "Human readable model name"
    },
    "model_provider_country": {
      "type": "text",
      "required": true,
      "description": "Provider company country"
    },
    "official_url": {
      "type": "text",
      "required": true,
      "description": "Official model/provider URL"
    },
    "input_modalities": {
      "type": "text",
      "required": true,
      "description": "Supported input modalities (Text, Image, Audio, Video)"
    },
    "output_modalities": {
      "type": "text",
      "required": true,
      "description": "Supported output modalities (Text, Image, Audio, Video)"
    },
    "license_info_text": {
      "type": "text",
      "nullable": true,
      "description": "License information text content (can be null)"
    },
    "license_info_url": {
      "type": "text",
      "nullable": true,
      "description": "License information URL (can be null)"
    },
    "license_name": {
      "type": "text",
      "required": true,
      "description": "License name/identifier"
    },
    "license_url": {
      "type": "text",
      "required": true,
      "description": "License terms URL"
    },
    "rate_limits": {
      "type": "text",
      "required": true,
      "description": "API rate limits information"
    },
    "provider_api_access": {
      "type": "text",
      "required": true,
      "description": "Provider API access URL"
    }
  },
  "excluded_fields": {
    "note": "The following fields are auto-managed by Supabase and excluded from this schema",
    "fields": ["id", "created_at", "updated_at"]
  },
  "indexes": [
    "model_provider",
    "inference_provider",
    "human_readable_name",
    "license_name"
  ]
}